'use client'

/**
 * Utility function to get the correct base path for assets
 * Now returns '/builtit-template' in both development and production
 * for consistent behavior across environments
 */
export const getBasePath = () => {
  return '/builtit-template'
}
